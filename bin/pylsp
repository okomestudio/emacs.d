#!/usr/bin/env bash
#
# Set `lsp-pylsp-server-command` to this script.
#

if ! $(pyenv prefix)/bin/pylsp ; then
    pip install python-lsp-server[all] python-lsp-black pyls-isort
    pip install flake8 flake8-docstrings pydocstyle[toml]
fi

exec pylsp -v $@
