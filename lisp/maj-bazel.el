;;; maj-bazel.el --- Bazel -*- lexical-binding: t -*-
;;; Commentary:
;;
;; Configure the major mode for Bazel.
;;
;;; Code:

(use-package bazel-mode)

(provide 'maj-bazel)
;;; maj-bazel.el ends here
